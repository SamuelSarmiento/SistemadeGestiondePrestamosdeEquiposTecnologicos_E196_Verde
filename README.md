
  # arquitectura

  This is a code bundle for arquitectura. The original project is available at https://www.figma.com/design/3Be3xTdmahN3C7IY6TWKG0/arquitectura.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  